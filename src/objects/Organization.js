export default class Organization {
	constructor() {
		this.orgID = null;
		this.displayName = "";
	}

	toString() {
		return JSON.stringify(this);
	}

	/* ########################### Getters ########################## */

	// get list of admins

	// get list of users

	/* ########################### Setters ########################## */
}

// for simplicity, every user will be part of the org with ID "default" which will have the sample/demo scenarios and students already set up
